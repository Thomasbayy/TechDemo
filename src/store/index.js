import Vue from 'vue';
import Vuex from 'vuex';
import formBuilder from '@/store/modules/FormBuilder';

Vue.use(Vuex);
export default new Vuex.Store({
  modules: {
    formBuilder,
  },
  state: {
  },
  mutations: {
  },
  actions: {
  },
});
